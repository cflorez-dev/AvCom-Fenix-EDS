// Versión simplificada para debugging
export default function decorate(block) {
  block.innerHTML = '<div><h1>Design System - Versión Simple (Funciona)</h1><p>Si ves esto, el bloque carga correctamente.</p></div>';
}
